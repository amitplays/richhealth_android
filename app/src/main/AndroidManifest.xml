<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- NOTE: package="" was removed; the code package now lives in app/build.gradle
         as `namespace "com.example.richhealth"` (required by AGP 8+). -->


    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.android.vending.BILLING" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Wearable/fitness data via Google Fit: step data needs activity recognition (API 29+). -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <!-- TODO: Remove android:usesCleartextTraffic once ApiConfig.BASE_URL is switched to https
         (it currently points at the local dev server http://192.168.2.114:5000). -->
    <application
        android:name="com.example.richhealth.MyApplication"
        android:allowBackup="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31" >

        <activity
            android:name=".Activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Activities.MainActivity"
            android:exported="false"/>

        <activity
            android:name=".Activities.LoginActivity"
            android:exported="false"/>

        <activity
            android:name=".Activities.SignupActivity"
            android:exported="false"/>

        <activity
            android:name=".Activities.OnboardingActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".Activities.AddDependentActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".Activities.DoctorSearchActivity"
            android:exported="false"/>
        <activity
            android:name=".Activities.HealthAnalysisActivity"
            android:exported="false"/>
        <activity
            android:name=".Activities.UsageStatusActivity"
            android:exported="false"/>
        <activity
            android:name=".Activities.DietaryInsightsActivity"
            android:exported="false"/>
        <activity
            android:name=".Activities.NutriCheckActivity"
            android:exported="false"/>
        <activity
            android:name=".Activities.HealthAssistantActivity"
            android:exported="false"/>
        <activity
            android:name=".Activities.NotificationsActivity"
            android:exported="false"/>
        <activity
            android:name=".Activities.HealthFeedActivity"
            android:exported="false"/>

        <activity
            android:name=".Activities.MedicalReportsActivity"
            android:exported="true"/>

        <activity
            android:name=".Activities.AddWorkout"
            android:exported="true"/>

        <activity
            android:name=".Activities.DailyCheckInActivity"
            android:parentActivityName=".Activities.MainActivity"
            android:exported="false"/>

        <!-- Must be exported: BOOT_COMPLETED and MY_PACKAGE_REPLACED are sent by the
             system (uid 1000), and an unexported receiver is not a valid delivery
             target for them. With exported="false" the alarms were never restored
             after a reboot, so reminders stayed dead until Home was opened again.
             The alarm itself is delivered by an explicit Intent, which does not
             depend on this flag. -->
        <receiver
            android:name=".Activities.CheckInNotificationReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <!-- Some OEM skins (Xiaomi, HTC) send this instead of BOOT_COMPLETED -->
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <!-- App updates also clear pending alarms -->
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Medication reminders. Exported for the same boot/update reasons as the
             check-in receiver above; alarm + action broadcasts are explicit Intents,
             which do not depend on this flag. -->
        <receiver
            android:name=".Activities.MedicationReminderReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <!-- Alarms are armed as absolute epoch times (RTC_WAKEUP), so a timezone or
                     clock change leaves them pointing at the OLD wall-clock time until each
                     one fires once. Re-arm on both so a user who travels keeps getting their
                     reminders at the right local time. -->
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
            </intent-filter>
        </receiver>


        <!-- Hands a camera app a content:// URI to write the captured photo into.
             Used by the Richie composer's "Take Photo" option (see file_paths.xml). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>